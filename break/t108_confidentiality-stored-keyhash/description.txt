The database stores each user's PBKDF2 key_hash in plaintext, and stor.c uses that same 32-byte value directly as the AES-256-GCM encryption key. Parsing enc.db gives user2's key_hash, IV, tag, associated data (user2||file2), and ciphertext, so user2/file2 can be decrypted without knowing the password.
