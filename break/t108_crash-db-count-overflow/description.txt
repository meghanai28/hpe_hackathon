db_load trusts the signed user_count read from enc.db and allocates user_count * sizeof(User) with no upper bound or overflow check. On the required 32-bit build, user_count 268435456 makes the allocation size wrap to zero, then fread writes one 304-byte User record into that undersized allocation and db_free frees corrupted heap metadata. This should crash while loading the malformed fresh database.
